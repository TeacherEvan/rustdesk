# Refactor plan — `src/server/connection.rs` in `TeacherEvan/rustdesk`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-04T22:32:22.719851+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/rustdesk` |
| path | `src/server/connection.rs` |
| size | 302.0 KB (309,251 bytes) |
| lines | 7,383 |
| generated | 2026-09-04T22:32:22.719880+07:00 |

### Top imports (sample)
  - _(none sampled)_

## Structural analysis

**Top-level consts:**
- `FAILURE_IDX_ID_WHITELIST` (L74)
- `ID_WHITELIST_FAILURE_DECAY_MINUTES` (L77)
- `SWITCH_SIDES_UUID_TTL` (L94)
- `TERMINAL_OS_LOGIN_FAILED_MSG` (L104)
- `TEST_DELAY_TIMEOUT` (L389)
- `SEC30` (L390)
- `H1` (L391)
- `MILLI1` (L392)

**Render branches:** 2 `return (` blocks at L4180, L5486

**Repeated literals (≥2×):**
- `unix-file-copy-paste` (×25)
- `123456789@example.com:21116` (×7)
- `misc.switch_sides_request` (×5)
- `custom-rendezvous-server` (×3)
- `Connection not allowed` (×3)
- `Failed to access remote {}. Please make sure it is reachable` (×2)
- `supported_privacy_mode_impl` (×2)
- `peer info supported_encoding: {:?}` (×2)

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `rustdesk`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Hoist repeated literal `'unix-file-copy-paste'` (×25) to a constant
- **Target:** String literal appears 25× in `src/server/connection.rs`. Extract to
  `./constants.ts` (already exists per imports) or co-located `strings.ts`.
- **Why:** Repeated literals indicate copy that should be localizable or reused.
- **Acceptance:** zero occurrences of the literal in `src/server/connection.rs` after the change;
  constant referenced via the barrel.
- **Validation:** `rg -F '<literal>' src/server/connection.rs` returns 0.
- **Evidence:** grep before/after.

### OBJ-002 — Move 11 top-level constants to a sibling constants module
- **Target:** Top-level `const <Name> = ...` blocks at L74+
  should live next to the component (e.g. `./constants.ts`).
- **Why:** Constants colocated with the page force every refactor to
  re-validate the data layer. Moving them removes a coupling edge.
- **Acceptance:** `src/server/connection.rs` no longer defines top-level `const`; imports them instead.
- **Validation:** type-check + lint.
- **Evidence:** `rg -n '^const [A-Z]' src/server/connection.rs` → 0.

### OBJ-003 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `src/server/connection.rs` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `src/server/connection.rs`.
- **Validation:** `rg -n '^import' src/server/connection.rs` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-004 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `src/server/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-005 — Reduce `src/server/connection.rs` below 3691 lines via cumulative extractions
- **Target:** After OBJ-001 → OBJ-008 land, the remaining render block should
  be a thin composition of named sub-components.
- **Goal:** `src/server/connection.rs` shrinks from 7,383 to ≤3691 lines.
- **Acceptance:** `wc -l` shows the reduction; behavior unchanged via tests + e2e.
- **Validation:** `pnpm run build && pnpm run test && pnpm run test:e2e` (full gate).
- **Evidence:** before/after line count + green build.

### OBJ-006 — Hardening pass 6
- **Target:** Tighten types / tighten prop drilling in `src/server/connection.rs` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `src/server/connection.rs` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `src/server/connection.rs` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `src/server/connection.rs` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `src/server/connection.rs` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `src/server/connection.rs` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `src/server/connection.rs` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `src/server/connection.rs` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `rustdesk`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-04-bigfile-rustdesk-connection.rs.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.
